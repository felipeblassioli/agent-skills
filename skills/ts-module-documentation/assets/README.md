# Assets

Templates for JSDoc/TSDoc blocks. 
Read the templates in `assets/templates/` and copy the structure when writing new documentation. Replace placeholder values with the specific context of the code.

Available templates:
- `templates/module-jsdoc.txt`
- `templates/function-jsdoc.txt`
- `templates/type-jsdoc.txt`