# Skill Contract

Use this before creating files.

## Proposed Skill Meaning

- Job: <one recurring job>
- Why a skill: <why this should be a skill instead of another surface>
- Cursor scope: <project `.cursor/skills/` / user `~/.cursor/skills/`>
- Source artifact: <new skill / migrated rule / migrated command / refactor of existing skill>
- In scope: <flat list>
- Out of scope: <flat list>
- Triggers: <phrases users would actually say>
- Anti-triggers: <phrases or situations that should route elsewhere>
- Related surfaces: <rules / commands / hooks / sibling skills>
- Invocation mode: <implicit / explicit-only via `disable-model-invocation: true`>
- Archetype: <Knowledge Hub / Tool Runner / Workflow Executor / Hybrid>
- Optional frontmatter:
  - `compatibility`: <needed / not needed>
  - `metadata`: <needed / not needed>

## Minimum File Plan

- `SKILL.md`: <why it exists>
- `references/`: <needed docs only>
- `assets/`: <needed templates/checklists only>
- `scripts/`: <only if deterministic execution is needed>

## Approval Gate

- Proceed to scaffold? <yes/no>
