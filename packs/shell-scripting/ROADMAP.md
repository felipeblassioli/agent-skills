# Roadmap

## Near Term

- Add a small smoke fixture that verifies installed Codex agents and skills are
  copied to the expected paths.
- Add focused references for reusable shell script templates if the compact
  skill hot paths are insufficient in practice.

## Medium Term

- Decide whether any shell-scripting skill should be promoted to a root skill
  with independent version authority.
- Consider a Cursor target variant only if the same adapted content is useful
  outside Codex installs.
