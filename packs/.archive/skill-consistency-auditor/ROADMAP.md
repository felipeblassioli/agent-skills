# Roadmap

## Known gaps and upcoming work

- **v0.2.0**: Add a `skill-usage-analyzer` subagent that parses `agent-transcripts/` to find skills that load frequently but are never followed.
- **v0.2.0**: Integrate an optional `--fix` profile that allows the advisor to automatically draft the required anti-triggers or rename operations.
