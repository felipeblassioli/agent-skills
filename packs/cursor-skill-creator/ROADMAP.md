# Roadmap

- Add a deterministic workspace bootstrap script for initializing eval folders and
  metadata files before the review loop starts.
- Adapt the remaining Claude-only description-optimization flow into a
  Cursor-friendly orchestration path.
- Add more bundled templates for pack guides, rule stubs, and eval assertions.
- Validate the pack in a second repository after local verification to confirm
  installed-path assumptions hold outside this repo.
