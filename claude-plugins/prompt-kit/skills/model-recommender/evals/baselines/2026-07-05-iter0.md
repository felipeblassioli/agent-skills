# model-recommender — eval baseline (iteration 0, bootstrap)

- **Date:** 2026-07-05
- **Eval suite:** [`../evals.json`](../evals.json) — **6** cases (4 routing, 2 controls: no-over-escalation, graceful-staleness).
- **Method:** each case resolved **with the skill** against the installed
  `~/.claude/model-profiles.md` (the routing is deterministic given the file:
  classify via `routing_rubric.when` → take tier + effort → resolve
  `tier_to_model[tier]`). This is a single traced pass, **not** a multi-run
  benchmark.
- **Baseline (without-skill) run:** **NOT executed.** No with-vs-baseline delta
  is claimed here — that is the iteration-1 item.

## With-skill results (traced against model-profiles.md @ last_reviewed 2026-07-05)

| # | Case | Verdict produced | Pass |
|---|------|------------------|------|
| 1 | build-a-plugin | deliberation / xhigh / `claude-opus-4-8`; no escalation | ✅ |
| 2 | locate-code | recon / low / `claude-haiku-4-5-20251001` | ✅ |
| 3 | scoped-implementation | execution / high / `claude-sonnet-5` | ✅ |
| 4 | phased-task | plan→opus, implement→sonnet, review→opus (per phase) | ✅ |
| 5 | no-over-escalation (control) | deliberation / `claude-opus-4-8`, **not** fable | ✅ |
| 6 | graceful-staleness (control) | deliberation / high emitted; string flagged stale, not guessed | ✅ (by design; not yet run under a genuinely stale file) |

6/6 traced pass with the skill. Case 6 is asserted from the skill's stated
degradation rule; it has not been exercised against an actually-stale file.

## Honest gaps (iteration-1 work)

- No baseline (without-skill) comparison → no measured delta / uplift.
- Single deterministic trace, not a multi-run agent benchmark; no stddev, time,
  or tokens captured.
- Case 6 needs a real stale/unreachable-file run to confirm the flag-not-guess
  behavior end to end.
- No `.benchmark.json` machine-readable snapshot yet (the blassioli convention
  pairs one with the `.md`).
