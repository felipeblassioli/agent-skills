# prompt-audit — eval baseline (iteration 0, bootstrap)

- **Date:** 2026-07-05
- **Eval suite:** [`../evals.json`](../evals.json) — **8** cases (5 true-positive
  rule fires, 2 calibration controls, 1 dependency).
- **Method:** the suite is specified; one **real dogfood run** was executed this
  session (below). The remaining crafted cases are specified but **not yet run**
  as a with-vs-baseline benchmark.
- **Baseline (without-skill) run:** **NOT executed.** No delta claimed.

## Real run executed this session

**Target:** `loop-compiler`'s `skills/compile-plan/SKILL.md` (the operative,
outcome-oriented loop-compiler prompt — there is no separate "bootstrap prompt"
file). This is the dogfood the rule set's own self-consistency check calls for.

- **Result:** **1 finding** — a soft `[R2-prescription-posture | warn→nit]` (the
  7 explicit steps could collapse to a briefer instruction under a Fable target).
  **No `block`s. Not rewritten wholesale.**
- **Calibration held:** the ~15 "never/do not" statements in loop-compiler
  (no branch/commit/merge/deploy, never invent, never overwrite) did **not**
  fire R4 — they are safety prohibitions, not steering negatives. This is the
  behavior control case #5 asserts, confirmed on real input.
- **Verdict:** ≤2 findings, brief preserved. Matches the "a calibrated audit
  mostly passes a well-written prompt" expectation.

## Honest gaps (iteration-1 work)

- Cases 1–4, 7, 8 are specified but not yet executed; cases 5–6 (calibration)
  are asserted from the one real run's behavior, not from running those exact
  prompts.
- No baseline (without-skill) comparison → no measured uplift.
- No multi-run variance, time, or tokens; no `.benchmark.json` snapshot yet.
- Next: run all 8 cases with-skill vs baseline and record the delta, especially
  the two calibration controls (must NOT over-flag).
