# smart-prompt — eval baseline (iteration 0, bootstrap)

- **Date:** 2026-07-05
- **Eval suite:** [`../evals.json`](../evals.json) — **6** cases (3 archetype
  matches, 1 no-fit control, 1 shape-not-run control, 1 growth-loop dependency).
- **Method:** the suite is specified; one **real trace** was executed this
  session (below, the flagship). The remaining cases are specified but **not yet
  run** as a with-vs-baseline benchmark.
- **Baseline (without-skill) run:** **NOT executed.** No delta claimed.

## Real trace executed this session

**Case 1 (flagship, `context-anchored-planning`).** Ran the shaping on the exact
loose intent in the design brief:

> read refs/README.md and study gsd-core and obra/superpowers … to know how they
> gather context to anchor an implementation (plan → implementation plan)

- **Match:** `context-anchored-planning` selected from `intent_signals`
  ("study X and Y to learn how they do Z, then plan").
- **Routing:** `deliberation` overall; phased as recon/low (gather the exemplars)
  → deliberation/high (extract the pattern, plan). Resolved via model-recommender;
  no model string written into the emitted prompt. ✔ matches the archetype.
- **Shape produced:** read-only-first, cite-anchors (file:line from both
  projects), a clarify-first gate, and an explicit plan-not-code output contract
  with an acceptance condition — the before/after recorded in
  `references/prompt-archetypes.md`.
- **Discipline:** emitted a prompt; did **not** itself read the repos or produce
  the plan. ✔ shape-not-run held.

## Honest gaps (iteration-1 work)

- Cases 2–6 are specified but not yet executed; only case 1 was run end to end.
- No baseline (without-skill) comparison → no measured uplift.
- The growth-loop (case 6) is specified but the ledger has not been exercised on
  a real capture yet; `~/.claude/smart-prompt-ledger.md` does not exist until the
  first real win is captured.
- No promotion has happened yet — all five seed archetypes are hand-authored from
  the method; only `context-anchored-planning` is backed by a real trace. The
  other four are `provenance: validated` by construction of the seed set and
  should earn a real case before the next version.
- Next: run all 6 cases with-skill vs baseline; exercise one real capture →
  promotion round so the loop is proven, not just specified.
