# tailor-to-fable — eval baseline (iteration 0, bootstrap)

- **Date:** 2026-07-14
- **Eval suite:** [`../evals.json`](../evals.json) — **6** cases (flagship
  transform, reasoning-echo block, rough-idea elevation, rejected-params,
  refusal-caution, anti-over-tailoring control).
- **Method:** the suite is specified; one **real trace** was executed this
  session (below, the flagship). The remaining cases are specified but **not yet
  run** as a with-vs-baseline benchmark.
- **Baseline (without-skill) run:** **NOT executed.** No delta claimed.
- **Profile used:** `~/.claude/model-profiles.md` fable-5 block,
  `last_verified 2026-07-05` (fresh: 9 days, inside the 30-day window).

## Real trace executed this session

**Case 1 (flagship, un-prescribe-and-unleash).** Ran the transform on the
over-prescribed step-script prompt in the suite.

- **Target:** resolved `tier_to_model.escalation` → `claude-fable-5`; no model
  string written into the emitted prompt. ✔
- **M1:** the four numbered steps collapsed to a framed problem — goal (remove
  the auth god-object), constraints (behaviour-preserving), acceptance — with the
  method left open. ✔
- **M2/M7:** "think very hard" and "be extremely thorough" removed as worded
  effort; "summarize every 3 files" removed as stale scaffolding; run config
  emitted instead (`effort xhigh, thinking adaptive`). ✔
- **M3:** explicit boundaries + a progress-audit line added for the long run. ✔
- **Discipline:** emitted a prompt; did **not** perform the refactor. ✔
- **Gate:** prompt-audit run on the result — clean (no R3 block; R2/R6/R7 that
  fired on the *input* were the edits applied). ✔

## Honest gaps (iteration-1 work)

- Cases 2–6 are specified but not yet executed; only case 1 was run end to end.
- No baseline (without-skill) comparison → no measured uplift.
- Case 2 (reasoning-echo → silent-fallback) is the highest-value regression to
  run live next, since a miss there produces a run that *looks* like Fable but is
  Opus.
- Case 6 (anti-over-tailoring) guards against the classic prompt-tool failure —
  gutting a good prompt to look busy; run it to confirm the transform stays a
  small diff on already-strong input.
- The `references/fable-playbook.md` snippet pointers link to
  `profile.source_url`; they are not verified against the live page this session
  beyond the profile's own `last_verified`.
