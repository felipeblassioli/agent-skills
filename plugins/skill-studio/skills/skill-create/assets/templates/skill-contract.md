# Skill Contract

Use this before creating files. Get it approved, then scaffold.

## Proposed Skill Meaning

- Job: <one recurring job>
- Why a skill: <why this should be a skill instead of a subagent / command / hook / plugin>
- Ship shape: <standalone skill / one skill inside a plugin with sibling surfaces>
- Tier: <sandbox plugin (unproven) / official plugin (earned traction)>
- Source: <new skill / migrated artifact / refactor of existing skill / import>
- In scope: <flat list>
- Out of scope: <flat list>
- Triggers: <phrases users would actually say>
- Anti-triggers: <phrases or situations that should route elsewhere, by name>
- Related surfaces: <sibling skills / subagents / commands / hooks>
- Archetype: <Knowledge Hub / Tool Runner / Workflow Executor / Hybrid>
- Source contracts: <external tools/libraries/runtimes this teaches, or none>

## Minimum File Plan

- `SKILL.md`: <why it exists; frontmatter = name + description only>
- `metadata.json`: <version/author/date/abstract/source_contracts>
- `CHANGELOG.md`: <matching-version entry>
- `references/`: <needed docs only>
- `assets/`: <needed templates/checklists only>
- `scripts/`: <only if deterministic execution is needed>

## Approval Gate

- Proceed to scaffold? <yes/no>
