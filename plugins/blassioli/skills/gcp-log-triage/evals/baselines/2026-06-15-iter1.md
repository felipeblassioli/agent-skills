# gcp-log-triage — eval baseline (iteration 1)

- **Date:** 2026-06-15
- **Model:** claude-opus-4-8
- **Eval suite:** [`../evals.json`](../evals.json) — 8 edge cases (4 traps 🪤, 2 discrimination controls 🔀, 2 over-trigger controls ✅)
- **Method:** each case run twice — with the skill active vs. baseline (no skill) — graded strictly per-assertion with quoted evidence ([agentskills.io/skill-creation/evaluating-skills](https://agentskills.io/skill-creation/evaluating-skills)).
- **Machine-readable:** [`2026-06-15-iter1.benchmark.json`](2026-06-15-iter1.benchmark.json)
- **Raw runs:** `.work/gcp-log-triage-workspace/iteration-1/` (gitignored — reproduce from the suite above).

## Summary

| Metric | With skill | Baseline | Delta |
|--------|------------|----------|-------|
| **Assertion pass rate** | **92.9%** (26/28) | **57.1%** (16/28) | **+35.7 pts** |

## Per edge case (with → baseline)

| Edge case | Type | With | Base | What the delta shows |
|---|---|---|---|---|
| noisy-export-collapse-to-signal | 🪤 | 4/4 | 3/4 | Baseline finds the error but misses the repeatable dedup-by-trace tooling/funnel (no `triage-logs.sh`). |
| gke-archetype-targeting | 🪤 | 3/4 | 3/4 | **No delta** — both target `k8s_container`/plain `stderr`/project/severity, **both miss** the GKE no-trace correlation note. |
| secret-leak-apikey | 🪤 | 4/4 | 3/4 | Baseline masks in the logger but never reaches for `--format` field projection as the cheap redaction. |
| exclusion-filter-safety | 🪤 | 4/4 | 2/4 | Baseline gives a correct `_Default` command but never warns exclusions are unrecoverable; treats a blanket drop as the default. |
| defer-rootcause-to-sibling | 🔀 | 3/3 | 0/3 | Baseline names no sibling and absorbs the code-mapping job; skill hands off to `error-trace-rootcause` by name. |
| defer-ranking-to-sibling | 🔀 | 2/3 | 1/3 | Skill routes ranking to `error-reporting`; baseline builds a full raw-log aggregation. (See known flaw 2.) |
| cloudrun-logname-encoding | 🪤 | 3/3 | 3/3 | **No delta** on the graded criteria (both nail `%2Fstderr`); skill adds an unprompted Bearer-header security note. |
| overtrigger-control-instrumentation | ✅ | 3/3 | 1/3 | Baseline answers the out-of-scope OTel-authoring question in full; skill recognizes over-trigger → app-side logging guidance. |

## Known flaws (iteration-2 candidates)

1. **`gke-archetype-targeting` (3/4):** on a plain "write a query" ask, the skill did not proactively surface the GKE "no top-level trace → use `jsonPayload.traceId`" note (the fact lives in `service-archetypes.md` but wasn't volunteered). Consider nudging it in the SKILL.md Locate step. Not a safety issue.
2. **`defer-ranking-to-sibling` (2/3):** the skill correctly routes ranking to `error-reporting` by name, but still used raw logs as a sanity check, so the strict "does not compute rankings from raw logs" assertion failed. Minor over-eagerness, not a mis-route.

No safety/secret assertion failed with-skill (secret-leak 4/4, exclusion-safety 4/4).
